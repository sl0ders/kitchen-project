{% extends 'admin/base.html.twig' %}
{% trans_default_domain 'kitchen-project' %}

{% block title %}{{ 'title.notify.edit'| trans({"%object%" : notify.id})  }}{% endblock %}

{% block body %}
    <h1 class="text-center mt-5">{{ 'title.notify.edit'| trans({"%object%" : notify.id})  }}</h1>

    {{ include('admin/notify/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('admin_notify_index') }}">back to list</a>

    {{ include('admin/notify/_delete_form.html.twig') }}
{% endblock %}
